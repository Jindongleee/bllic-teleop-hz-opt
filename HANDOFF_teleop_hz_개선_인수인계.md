# 텔레옵 수집 Hz 개선 인수인계 (BLLIC etri_eai_sim)

> 작성: 2026-08-12, 이진동. **받은 zip은 개선 적용 전 스냅샷**이므로 아래 7건을 이식하면 됩니다.
> 이 문서는 Claude(또는 다른 AI 에이전트)에게 그대로 넘겨서 작업시킬 수 있게 작성했습니다.
> 결과 요약: 텔레옵 수집 주파수 **14.76 → 22.2±0.4 Hz** (업체 데이터 29.1Hz의 0.76배). 4코어 Xeon 기준.

## 0. 시스템 구조 (전제 지식)

- **sim** (`etri_eai_sim-main/teleop.py`): Isaac Sim 시뮬 + 데이터 수집. 메인 루프 1회전 = 물리 4스텝(sim dt 5ms×4=20ms) + 렌더 + 저장 = 저장 프레임 1개. **레이트 리미터가 무력화돼 있어 수집 Hz = 그 PC가 내는 속도.**
- **C** (`xr_teleoperate/teleop/teleop_hand_and_arm.py`): Quest3 XR 조종·IK. 30Hz 송신, 병목 아님.
- **오프라인 렌더** (`render_final.py`): 수집한 `sim_state_raw/*.pt`를 재생해서 영상 생성. **수집 단계는 이미지를 저장하지 않는 게 정상 설계** (`colors={}`).
- ⚠️ **하드 제약**: `data.json.gz` 스키마·`sim_state_raw/*.pt` 구조·"1제어스텝=1프레임" 정합은 절대 불변 (render_final·LeRobot 변환이 깨짐).

## 1. 측정 방법부터 (이거 틀리면 전부 무효)

- **유일 지표 = 저장 프레임 수 ÷ 실제 녹화(REC) 시간.** C 로그의 `[record] ⏱` 줄이 REC 시간을 줌.
- **쓰면 안 되는 지표** (우리가 세 번 데임): `overall average frequency`(유휴 포함), `moving average`, dt 중앙값(이봉분포), 프로파일러 소계(≠실제 루프).
- 런 간 편차가 ±0.6Hz이므로 **A/B 판정 기준은 +1.0Hz 이상**, 각 조건 n≥2.
- **한 번에 한 변수만** 바꿀 것. 런마다 sim·C 모두 재시작.
- 참고: `info.fps=30`은 측정값이 아니라 **하드코딩 라벨**. 수집 Hz와 무관하며 LeRobot timestamp가 `index/30`으로 생성되므로 건드리지 말 것.

## 2. 적용할 개선 7건 (효과 큰 순)

### ① 카메라 파이프라인 차단 (+3.7Hz, 최대 단일 이득)

- **문제**: 아무도 안 보는 카메라 영상 체인이 프레임당 ~16ms 소모. 비용이 프로파일러 밖(비동기 스레드·별도 프로세스)에 숨어 있음: `camera_state.py::_async_writer_loop`(shm 기록 스레드) + `image_server` 프로세스 + C의 `ImageClient` 스레드.
- **적용**: 수집 중 ⓐ image_server를 띄우지 않는다 ⓑ shm 스트리밍·카메라 센서 갱신을 환경변수로 차단한다(우리 구현: `TELEOP_NO_IMAGE_STREAM=1`) ⓒ C 쪽 `STREAM_VR_VIDEO=0`.
- **주의**: 카메라 센서(`CameraCfg`)는 **존재만으로** 렌더 패스를 만든다 — ObsTerm 게이팅으로는 못 막음. 해상도를 1×1로 줄여도 1ms밖에 안 줄어듦(비용은 렌더 패스 존재 자체).
- **의외의 사실**: 1인칭 헤드캠 뷰가 3인칭 뷰포트보다 **빠름** (렌더 비용은 카메라 개수가 아니라 화면에 보이는 오브젝트 양이 결정). 수집 데이터에는 영향 0 (영상은 render_final이 오프라인 생성).
- **검증**: 저장 프레임 스키마 불변 + Hz 상승.

### ② USD 트리 재순회 캐싱 (−7.1ms)

- **문제**: `action_provider/action_provider_wh_dds.py`의 저장 블록이 **매 프레임** `stage.GetPrimAtPath` → `GetChildren()`(오브젝트 95개) → `Usd.PrimRange` 전수 순회 + `UsdPhysics.*API` 타입 체크. 저장 10ms 중 **9.4ms가 "데이터가 어디 있는지 찾기"**, 실제 수집은 0.6ms.
- **적용**: 씬 로딩 후 오브젝트 목록은 안 바뀌므로 **경로·rb_view 인덱스를 1회 구축해 캐싱** (`rb_view`는 이미 캐싱돼 있는데 경로 탐색만 안 돼 있음). 캐시 전/후 저장 결과(키·shape·dtype·값) 동등성 스크립트로 검증할 것.
- **효과**: 저장 블록 9.96 → 2.85ms.

### ③ 렌더 간격 2→3 (−9.6ms)

- **문제**: 기본 `render_interval=2` (2스텝마다 렌더). 렌더 1회 ~30ms급.
- **적용**: 3스텝마다 렌더로 변경. ⚠️ **`--render_interval` CLI 인자는 죽은 인자** — 실제로 안 읽힘. env cfg의 `sim.render_interval`을 직접 바꾸거나 환경변수 게이트를 새로 달 것.
- **대가**: 조종 화면 갱신 ~7Hz로 감소. 우리는 파지 실기까지 통과했지만 **조종성 실기 확인 필수.**
- 4로 더 줄이는 건 화면이 너무 끊겨서 보류 권장.

### ④ ⭐ vuer 라이브러리 busy-spin 패치 (+2.9Hz, 한 줄)

- **문제**: C 쪽 televuer 자식 프로세스(Quest3 XR 서버)가 **코어 1개를 100% 점유.** 원인은 vuer 라이브러리 자체: `vuer/server.py`의 `uplink` 코루틴이 보낼 메시지가 없을 때 `await sleep(0.0)` — 이벤트루프에 양보만 하고 즉시 재실행되는 busy-spin. 헤드셋에 영상을 안 보내는 구성(STREAM_VR_VIDEO=0)에선 큐가 거의 항상 비어 있어 상시 스핀.
- **적용**: vuer가 설치된 site-packages에서 `uplink` 루프의 **유휴 분기만** `await sleep(0.002)`로 변경 (vuer 0.0.32RC7 기준 server.py:1125 부근, `else: await sleep(0.0)`). 큐에 메시지가 있으면 슬립 없이 연속 송신하므로 처리량 영향 0.
  - ⚠️ 같은 파일에 `sleep(0.0)`이 두 곳 더 있는데(이벤트 도착 시에만 실행됨) 그건 스핀이 아니니 **건드리지 말 것.**
  - 원본 백업 필수 (`server.py.bak_날짜`).
- **진단법(선택)**: 조종 중 televuer PID에 `sudo py-spy dump` → 스택이 `uplink → sleep`이며 CPU 100%면 동일 증상.
- **효과**: 코어 1개가 통째로 반환 → 물리 스텝이 23.3→20.4ms로 동반 하락(경합 해소). 코어가 8개 이상인 머신이면 효과가 작을 수 있음.

### ⑤ 팔 속도 리미터 (Hz가 아니라 데이터 품질용)

- **문제**: 수집 Hz가 낮으면 프레임 간 조종자 손 이동이 커져 **팔 목표가 프레임당 크게 점프** → WBC(하체)가 외란을 받아 몸통·헤드캠이 흔들림. 흔들림은 물리에 구워져서 재생 속도를 바꿔도 안 사라짐.
- **분석 결론** (팔 활동량을 맞춘 조건부 비교로 확증): **보행 정책 자체는 무죄.** 같은 팔 입력이면 다리 반응은 업체 데이터와 동급. 원인은 순수하게 입력(Hz) 쪽.
- **적용**: 관절별 프레임당 |Δq| 상한 클램프 (우리 구현: `ARM_RATE_LIMIT=1`). 상한은 업체 8에피소드 실측 max (rad/프레임):
  `shoulder_pitch 0.0566 · shoulder_roll 0.0487 · shoulder_yaw 0.1162 · elbow 0.1302 · wrist_roll 0.0911 · wrist_pitch 0.1693 · wrist_yaw 0.1223`
- 평상 움직임은 안 건드리고 급발진 스파이크만 자름. 평균적 시간압축은 리미터로 안 없어짐 — **Hz를 올리는 것만이 근본 해법.**

### ⑥ 측정·운영 인프라 (작지만 필수)

- 프로파일러(`[prof-sim]` 등 100스텝 평균 출력)를 넣고 **기본 ON** 유지 — 오버헤드 0.001%인데 이게 없으면 녹화 구간을 못 잼.
- 런마다 **설정 지문**(`last_run_config.json`)을 남길 것 — "어떤 설정으로 돌렸는지 기록이 없어" 결과를 통째로 버린 적 있음.
- 로그 파일명에 런 이름을 넣어 덮어쓰기 방지.

### ⑦ (해당 시) 텔레옵 이중발행 가드

zip 시점에 따라 이미 있을 수 있음: C 시작 전 좀비 `teleop_hand_and_arm.py` 정리(`pgrep` 가드) + DDS를 `lo` 인터페이스로 격리(`ChannelFactoryInitialize(1,"lo")`). 없으면 두 프로세스가 같은 토픽에 팔 명령을 동시 발행해 로봇이 허우적거림.

## 3. ❌ 하지 말 것 (전부 실측으로 기각 — 시간 낭비 방지)

| 시도 | 결과 |
|---|---|
| GPU 물리 (`device="cuda"`) | **3.8배 악화.** num_envs=1이라 GPU 고정비만 뭄. 업체의 `cpu`가 옳음 |
| `PHYSICS_DT` 변경 | 걷기 정책·PD게인이 전부 dt=5ms 전제라 보행 붕괴 |
| RT 레이트레이싱 캐싱 | 효과 없음 (움직이는 씬) |
| CPU 코어 핀닝 (taskset) | ④ 적용 후엔 초과구독이 해소돼 효과 없음 |
| `OMP_NUM_THREADS` 축소 | 효과 없음 (같은 이유) |
| **저장을 별도 프로세스로** | **−3Hz 악화.** pickle이 GIL을 통째로 잡는 반면 torch.save는 C 구간에서 GIL을 풀어줌 |
| multiprocessing (spawn/forkserver) 일반 | ⚠️ **이 코드베이스에서 원천 불가** — 자식이 `__main__`(teleop.py)을 재-import하는데 `AppLauncher`가 모듈 레벨(:142)이라 자식이 Isaac을 통째로 재기동함. 자식 프로세스가 필요하면 `subprocess.Popen`으로 독립 스크립트를 띄울 것 |
| `SKIP_SIM_STATE_RAW=1` | Hz는 오르지만 render_final 입력이 사라져 **본 수집에 절대 금지** (측정 전용) |
| 프레임 서브샘플링/`info.fps` 수정 | 데이터 양식 파괴 — 금지 |

## 4. 함정 모음

1. **`${VAR:-기본값}` 함정**: 셸 스크립트에서 `VAR=""`로 껐다고 생각했는데 기본값이 되살아나 실제로는 켜져 있던 사고. 지문 기록으로 방어.
2. **C가 죽거나 재접속해도 sim 녹화 게이트가 안 닫힘** — 유휴 프레임이 같은 에피소드 폴더에 계속 쌓여 오염됨. 런 사이엔 sim도 재시작할 것.
3. **teleop_c.log는 세션마다 덮어써짐** — 과거 에피소드의 REC 시간은 dds 로그의 `metadata.step` ↔ `sim_state_raw/*.pt` 스텝 조인으로 역산.
4. 렌더 비용은 **화면에 보이는 양**에 따라 런마다 ±2ms 출렁임 — A/B 때 시야도 비슷하게 유지.

## 5. 우리 쪽 최종 상태 (참고 기준선)

| 단계 | ★Hz |
|---|---|
| 시작 (8/7) | 14.76 |
| +① 카메라 차단 | 18.4 |
| +② USD 캐싱 (8/11 기준선) | 18.5 |
| +③ ri3 +⑤ 리미터 | 19.3 |
| **+④ vuer 패치 (최종)** | **22.2±0.4 (최고 22.55)** |
| 업체 | 29.10 |

프레임당 예산(최종): 물리 20.4 + 렌더 9.6 + 저장 2.6 + 미계측 ~12.5 = ~45ms.
남은 병목은 미계측 12.5ms(DDS 발행·컨트롤러·파이썬 오버헤드) — 분해 계측까지가 우리 진행분.

## 6. 원본 파일 위치 (구현 참고가 필요하면 요청)

이진동 머신 `/media/jindong/Data/BLLIC_teleoperate/etri_eai_sim-main/` (①②⑤⑥ 구현),
`~/bllic_ws/pitcher_task/` (`collect_episode.sh`·`measure_hz.py`·`contention_monitor.py`·`analyze_shake_transfer.py`),
vuer 패치는 conda `tv` env의 `site-packages/vuer/server.py`.
상세 실측 기록은 옵시디언 "09 텔레옵 수집 Hz 규명" 폴더 (필요 시 export 가능).
